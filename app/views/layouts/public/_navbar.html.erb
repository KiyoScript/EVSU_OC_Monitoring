<% unless user_signed_in? || request.path == new_user_session_path %>
  <header class="header header-sticky position-static p-0 border-0">
    <div class="container-fluid px-4 justify-content-center">
      <%= link_to root_path, class: "text-decoration-none" do %>
        <h3 class="fw-bolder text-primary">EVSU-OC Monitoring System</h3>
      <% end %>
    <div>
  </header>
<% end %>